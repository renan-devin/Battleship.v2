/**
 * AI layer: pure logic, no DOM, no framework.
 *
 * Future home of the opponent strategies (easy random targeting and hard
 * hunt/target targeting). Modules in this layer only consume engine data
 * structures and return moves.
 */

export const DIFFICULTIES = ['easy', 'hard'];
