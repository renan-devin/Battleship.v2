/**
 * Game engine layer: pure logic, no DOM, no framework.
 *
 * Future home of board representation, ship placement and shot resolution.
 * Modules in this layer must stay free of browser APIs so they can be unit
 * tested in isolation.
 */

export const BOARD_SIZE = 10;
